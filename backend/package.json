{
  "name": "backend",
  "version": "1.0.0",
  "main": "src/server.js",
  "scripts": {
    "dev": "nodemon src/server.js",
    "start": "node src/server.js",
    "db:sync": "node src/scripts/sinkronDatabase.js",
    "db:sync:force": "node src/scripts/sinkronDatabase.js --force",
    "db:seed": "node src/scripts/isiDataAwal.js",
    "db:import-excel": "node src/scripts/imporExcelZona.js",
    "test": "node --test test/suite.test.js",
    "test:syntax": "node -e \"const fs=require('fs');const path=require('path');function walk(d){for(const f of fs.readdirSync(d)){const p=path.join(d,f);if(fs.statSync(p).isDirectory())walk(p);else if(p.endsWith('.js'))require('child_process').execFileSync(process.execPath,['--check',p],{stdio:'inherit'});}}walk('src');\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "bcryptjs": "^3.0.3",
    "cors": "^2.8.6",
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "express-rate-limit": "^8.6.2",
    "helmet": "^8.3.0",
    "jsonwebtoken": "^9.0.3",
    "multer": "^2.2.0",
    "mysql2": "^3.23.4",
    "qrcode": "^1.5.4",
    "sequelize": "^6.37.8",
    "xlsx": "^0.18.5"
  },
  "devDependencies": {
    "nodemon": "^3.1.14"
  }
}
